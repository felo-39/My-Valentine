<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Be My Valentine ❤️</title>

  <link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      overflow: hidden;
    }

    /* subtle background photo */
    .bg-photo {
      position: absolute;
      inset: 0;
      background-image: url('her-photo.jpg');
      background-size: cover;
      background-position: center;
      filter: blur(6px) grayscale(20%);
      opacity: 0.2;
      z-index: 0;
    }

    .card {
      position: relative;
      z-index: 1;
      background: white;
      padding: 2.5rem 3rem;
      border-radius: 26px;
      text-align: center;
      max-width: 420px;
      box-shadow: 0 20px 45px rgba(0,0,0,0.15);
      animation: float 3s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50% { transform: translateY(-10px); }
    }

    h1 {
      font-family: 'Pacifico', cursive;
      font-size: 2.6rem;
      color: #ff4d6d;
      margin-bottom: 0.75rem;
    }

    p {
      font-size: 1.05rem;
      color: #555;
      margin-bottom: 0.75rem;
    }

    .love-message {
      font-size: 0.95rem;
      color: #666;
      line-height: 1.6;
      margin: 0.75rem 0 1.25rem;
      opacity: 0;
      transform: translateY(6px);
      animation: fadeInUp 1.2s ease forwards;
      animation-delay: 0.3s;
    }

    @keyframes fadeInUp {
      to { opacity: 1; transform: translateY(0); }
    }

    .buttons {
      display: flex;
      justify-content: center;
      gap: 1rem;
      margin-top: 0.5rem;
    }

    button {
      border: none;
      padding: 0.8rem 1.6rem;
      border-radius: 30px;
      font-size: 1rem;
      cursor: pointer;
      transition: all 0.25s ease;
    }

    .yes { background: #ff4d6d; color: white; }
    .yes:hover { background: #e63e5c; transform: scale(1.05); }
    .no { background: #f1f1f1; color: #333; }

    .response {
      margin-top: 1.25rem;
      font-size: 1.15rem;
      font-weight: 600;
      display: none;
    }

    .yes-response { color: #ff4d6d; }
    .no-response { color: #777; }

    .float {
      position: absolute;
      animation: rise 4s linear forwards;
      pointer-events: none;
    }

    @keyframes rise {
      from { transform: translateY(0) scale(1); opacity: 1; }
      to { transform: translateY(-120vh) scale(1.6); opacity: 0; }
    }

    /* final screen */
    .final {
      position: fixed;
      inset: 0;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      display: flex;
      align-items: center;
      justify-content: center;
      z-index: 5;
      opacity: 0;
      pointer-events: none;
      transition: opacity 0.6s ease;
    }

    .final.show { opacity: 1; pointer-events: auto; }

    .final .box {
      background: rgba(255,255,255,0.95);
      padding: 2.5rem 3rem;
      border-radius: 26px;
      text-align: center;
      box-shadow: 0 20px 45px rgba(0,0,0,0.15);
    }

    .final h2 {
      font-family: 'Pacifico', cursive;
      color: #ff4d6d;
      font-size: 2.4rem;
      margin-bottom: 0.75rem;
    }
  </style>
</head>
<body>

  <!-- Background image -->
  <div class="bg-photo"></div>

  <!-- Background music (plays on YES click) -->
  <audio id="music" src="song.mp3"></audio>

  <div class="card">
    <h1>Gloria ❤️</h1>
    <p>Would you do me the honor of being my Valentine?</p>
    <div class="love-message">
      Hey <strong>Gloria</strong>, I have really tried to go for something extraordinary this year, but I couldn't think of one. I know this distance between us makes you feel forgotten, but I'm going to try everything I can to make you feel loved. This is just but the beginning, more is yet to come. I have something prepared for you when we meet, but first, accept my request.
    </div>

    <div class="buttons">
      <button class="yes" onclick="sayYes()">Yes 💖</button>
      <button class="no" onclick="sayNo()">No 🙈</button>
    </div>

    <div class="response yes-response" id="yesMsg">You just made me the happiest person alive 🥰</div>
    <div class="response no-response" id="noMsg">😢 It’s okay. Please give me a chance — I promise you won’t regret it.</div>

    <div class="response yes-response" id="yesMsg">You just made me the happiest person alive 🥰</div>
    <div class="response no-response" id="noMsg">😢 Ouch… that hurt a little</div>
  </div>

  <div class="final" id="finalScreen">
    <div class="box">
      <h2>I can’t wait to see you</h2>
      <div style="font-size:1.4rem">❤️ ❤️ ❤️</div>
    </div>
  </div>

  <script>
    const music = document.getElementById('music');
    music.volume = 0;

    function sayYes() {
      document.getElementById('yesMsg').style.display = 'block';
      document.getElementById('noMsg').style.display = 'none';

      music.play();
      let v = 0;
      const fade = setInterval(() => {
        if (v < 1) {
          v += 0.05;
          music.volume = v;
        } else {
          clearInterval(fade);
        }
      }, 200);

      for (let i = 0; i < 40; i++) {
        spawn('❤️');
        spawn('🌹');
        spawn('🎉');
      }

      setTimeout(() => {
        document.getElementById('finalScreen').classList.add('show');
        for (let i = 0; i < 25; i++) spawn('❤️');
      }, 1800);
    } else {
          clearInterval(fade);
        }
      }, 200);

      for (let i = 0; i < 40; i++) {
        spawn('❤️');
        spawn('🌹');
        spawn('🎉');
      }
    }

    function sayNo() {
      document.getElementById('noMsg').style.display = 'block';
      document.getElementById('yesMsg').style.display = 'none';
    }

    function spawn(symbol) {
      const el = document.createElement('div');
      el.className = 'float';
      el.innerHTML = symbol;
      el.style.left = Math.random() * 100 + 'vw';
      el.style.bottom = '-20px';
      el.style.fontSize = Math.random() * 22 + 18 + 'px';
      document.body.appendChild(el);
      setTimeout(() => el.remove(), 4000);
    }
  </script>

</body>
</html>
